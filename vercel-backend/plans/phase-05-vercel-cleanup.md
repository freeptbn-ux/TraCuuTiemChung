# Phase 05: Vercel Config & Cleanup

## Objective
Finalize và dọn dẹp.

## Tasks:
- [x] Cập nhật `vercel.json` (Rewrite tất cả về `api/index.go`).
- [x] Xóa code Python cũ (Sau khi đã backup).
- [x] Deploy lên Vercel Preview và chạy bộ test cuối cùng (User will push to GitHub).

## 🧪 Testing for this phase:
- **Smoke Test:** Chạy `vercel dev` và dùng `curl` gọi trực tiếp vào local server để check luồng từ đầu đến cuối.
