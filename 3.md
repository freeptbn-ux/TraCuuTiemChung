# Báo cáo Troubleshooting v3 - Tra Cứu Tiêm Chủng

## 1. Phân tích lỗi 404 Not Found (Vercel Logs)
Dựa trên log mới nhất từ Vercel:
`{"path":"/","status":404,"request_id":"38d8b7cf..."}`
`{"path":"/favicon.ico","status":404}`

- **Nguyên nhân**:
    - Dự án sử dụng `vercel.json` để rewrite mọi request (`/(.*)`) về `api/index.go`.
    - Tuy nhiên, trong router của Gin tại `api/index.go`, chúng ta mới chỉ định nghĩa các route bắt đầu bằng `/api/...`.
    - Khi trình duyệt hoặc app truy cập vào trang chủ (`/`) hoặc tự động tìm icon (`favicon.ico`), Gin không tìm thấy route khớp và trả về 404.
- **Hệ quả**: Làm nhiễu log, khiến chúng ta khó theo dõi các lỗi thực sự.

## 2. Các thay đổi đã thực hiện
Đã cập nhật `vercel-backend/api/index.go` để xử lý các request cơ bản này:

1. **Thêm Route Trang Chủ (`/`)**: 
    - Trả về JSON thông báo trạng thái "online" và lời chào. Giúp kiểm tra nhanh xem backend có sống không mà không cần qua `/api/health`.
2. **Xử lý Favicon (`/favicon.ico`, `/favicon.png`)**: 
    - Trả về status `204 No Content` thay vì `404`. Điều này ngăn trình duyệt báo lỗi khi cố tìm icon của trang web.
3. **Thêm `NoRoute` Handler**:
    - Khi có một request đến route chưa được định nghĩa, hệ thống sẽ log lại đường dẫn đó bằng `slog.Warn`. Giúp phát hiện sớm nếu app Android đang gọi sai endpoint.

## 3. Tối ưu hóa "Đúng chuẩn" Vercel Go Runtime
Sau khi nghiên cứu tài liệu chính thống về Go trên Vercel, tôi đã thực hiện một bước refactor quan trọng để đảm bảo hiệu năng tối ưu:

- **Sử dụng Warm Starts**: Thay vì khởi tạo lại toàn bộ Router Gin trên mỗi request (gây tốn tài nguyên và chậm), tôi đã chuyển Router thành biến toàn cục và khởi tạo trong hàm `init()`.
- **Cơ chế hoạt động**: Vercel sẽ giữ instance của function "ấm" (warm) trong một khoảng thời gian. Các request sau đó sẽ dùng ngay Router đã có sẵn trong bộ nhớ, giúp giảm latency đáng kể.
- **Cấu trúc Monolithic**: Gom toàn bộ logic routing vào một file entrypoint duy nhất (`api/index.go`) kết hợp với `vercel.json` rewrites. Đây là cách làm chuẩn nhất cho các framework như Gin khi chạy trên serverless.

## 4. Cập nhật về lỗi 400 và Dữ liệu rỗng
- **Lỗi 400**: Hiện tại trong log bạn gửi không thấy xuất hiện lỗi 400 nào mới. Điều này có thể do app Android đã gửi đúng format hoặc do log mới chỉ bắt được các request rác vào root.
- **Dữ liệu rỗng**: Các log chi tiết (`html_snippet`) đã được thêm ở bước trước. Khi bạn thực hiện một tra cứu thực tế trên App, hãy kiểm tra log Vercel để xem nội dung HTML phản hồi từ Portal VNCDC.

## 5. Hướng dẫn tiếp theo
1. **Deploy lại**: Chạy lệnh deploy để cập nhật các route mới và bản tối ưu hóa lên Vercel.
2. **Kiểm tra Hiệu năng**: Bạn sẽ thấy các request sau lần đầu tiên (cold start) sẽ phản hồi nhanh hơn nhiều.
3. **Kiểm tra Route**:
    - Truy cập trực tiếp URL của Vercel để thấy thông báo "online".
    - Kiểm tra log Vercel để đảm bảo không còn lỗi 404 cho favicon.
4. **Theo dõi Log Tra cứu**:
    - Khi app Android thực hiện tra cứu, hãy tìm log `html_snippet` để xem Portal VNCDC có đang chặn IP của Vercel hay không.

---
*Người thực hiện: Antigravity*
