# Báo cáo Troubleshooting v2 - Tra Cứu Tiêm Chủng

## 1. Phân tích lỗi 400 Bad Request
Dựa trên log Vercel:
`{"path":"/api/lookup","status":400,"error_message":""}`

- **Nguyên nhân**: Lỗi xảy ra tại bước `ShouldBindJSON` trong Gin. Điều này có nghĩa là request body gửi lên từ Android không hợp lệ (sai định dạng JSON, thiếu trường bắt buộc `phone`, hoặc sai `Content-Type`).
- **Khắc phục**: 
    - Đã bổ sung log chi tiết lỗi binding vào `index.go`. Nếu tiếp tục bị 400, log sẽ hiện rõ lý do (VD: `EOF` nếu body rỗng).
    - Đã đồng bộ trường `detail` trong response backend với Android app để hiển thị thông báo lỗi chính xác trên điện thoại thay vì "Lỗi máy chủ: 400".

## 2. Vấn đề "Không tìm thấy dữ liệu" (Status 200 nhưng data rỗng)
Mặc dù local test tìm thấy 2 bệnh nhân, nhưng trên Vercel lại trả về rỗng.

- **Kết quả Local Test**:
    ```
    Testing lookup for 0388634123 with user bn_dv_tcdvquevo
    Login successful
    Found 2 patients:
    - Nguyễn Duy Minh Khôi (ID: 24301480, DOB: 27/07/2020)
    - Nguyễn Duy Trường (ID: 74427304, DOB: 05/03/1992)
    ```
- **Phân tích**: Logic Go hoàn toàn đúng. Việc Vercel trả về rỗng thường do Portal VNCDC chặn các IP Datacenter hoặc yêu cầu Captcha khi phát hiện truy cập từ Vercel.
- **Hành động đã làm**: 
    - Thêm log `html_snippet` khi kết quả rỗng. Khi bạn chạy lại trên Vercel và xem log, chúng ta sẽ thấy nội dung HTML mà Portal trả về (là bảng trống, trang Login hay trang Block/Captcha).

## 3. Cấu hình Redis & API Key
- **Redis**: Log Vercel mới nhất cho thấy Redis đã kết nối thành công:
    `"msg":"redis connected","url":"rediss://default:gQAAAA...upstash.io:6379"`
    => Việc lắp ghép URL từ `UPSTASH_REDIS_REST_URL` và `TOKEN` là chính xác vì Upstash cho phép dùng token này làm password cho cả RESP.
- **API Key**: Đã xác nhận khớp giữa `local.properties` và biến môi trường (`@ijhfwfw2352$`).

## 4. Các thay đổi đã thực hiện
1. **`vercel-backend/api/index.go`**:
    - Thêm trường `detail` vào `StandardResponse` (Android app đang tìm trường này).
    - Thêm log chi tiết khi binding JSON lỗi (Fix 400).
    - Thêm log khi kết quả tìm kiếm rỗng (Debug empty data).
2. **`vercel-backend/pkg/portal/client.go`**:
    - Thêm log in ra 500 ký tự đầu của HTML khi không tìm thấy bệnh nhân để nhận diện trang bị block.
3. **`vercel-backend/pkg/config/config.go`**:
    - Tối ưu lại logic lấy `REDIS_URL`.

## 5. Kiến nghị tiếp theo
1. **Deploy lại code mới lên Vercel**.
2. **Thực hiện tra cứu lại** và theo dõi log Vercel.
3. **Kiểm tra `html_snippet` trong log**:
    - Nếu thấy HTML chứa "Security Check" hoặc "Captcha": Portal đã chặn IP Vercel. Cần dùng Proxy hoặc dùng server local/vps có IP "sạch" hơn.
    - Nếu thấy HTML là bảng trống nhưng local lại có: Portal có thể đang trả về dữ liệu khác nhau dựa trên session/IP.

---
*Người thực hiện: Antigravity*
