# Báo cáo Troubleshooting Tra Cứu Tiêm Chủng

## 1. Hiện trạng ứng dụng Android
- **Hành vi**: Khi nhấn "Tra cứu ngay" với số điện thoại `0388634123`, ứng dụng hiển thị thông báo: **"Không tìm thấy dữ liệu tiêm chủng."**
- **Logcat**: Không có lỗi crash. Log hệ thống cho thấy UI đã nhận sự kiện click. Việc hiển thị thông báo trên chứng tỏ ứng dụng đã nhận được phản hồi từ backend nhưng danh sách kết quả trả về bị rỗng.

## 2. Kiểm tra Backend (Vercel)
- **API Endpoint**: `https://tracuutiemchung.vercel.app/api/lookup`
- **Kết quả test trực tiếp (curl/python)**: 
  - Status: `200 OK`
  - Body: `{"status":"success","data":[],"request_id":"..."}`
- **Nhận xét**: Backend trên Vercel đang hoạt động nhưng không tìm thấy dữ liệu cho số điện thoại này từ hệ thống VNCDC.

## 3. Kiểm tra Logic Backend (Local Test)
- **Kịch bản**: Chạy script Go trực tiếp trên máy local với cùng thông tin tài khoản Portal (`bn_dv_tcdvquevo`) và số điện thoại `0388634123`.
- **Kết quả**: **Tìm thấy 2 bệnh nhân**:
  1. Nguyễn Duy Minh Khôi (ID=24301480, DOB=27/07/2020)
  2. Nguyễn Duy Trường (ID=74427304, DOB=05/03/1992)
- **Mâu thuẫn**: Tại sao Local chạy được mà Vercel lại trả về rỗng?

## 4. Nguyên nhân tiềm năng
### A. IP Blocking / Rate Limiting
- Hệ thống VNCDC có thể chặn các dải IP của AWS/Vercel hoặc yêu cầu Captcha khi phát hiện truy cập từ Datacenter.
- Khi bị chặn, Portal có thể trả về trang lỗi hoặc trang trắng, dẫn đến logic scraping trong `ParseSearchResults` không tìm thấy bảng dữ liệu và trả về danh sách rỗng `[]`.

### B. Lỗi cấu hình Redis (Session Sharing)
- Backend trên Vercel sử dụng Redis (Upstash) để lưu session cookie.
- Trong file `config.go`, URL Redis được lắp ghép từ `UPSTASH_REDIS_REST_URL`. Tuy nhiên, thư viện `go-redis` sử dụng giao thức RESP (port 6379), không phải REST (port 443).
- Test local với URL lắp ghép này bị **Timeout** (dial tcp ...:6379: i/o timeout).
- Nếu Redis không kết nối được, mỗi request lên Vercel sẽ phải thực hiện Login lại từ đầu. Nếu Portal chặn việc login liên tục từ 1 account/IP, nó sẽ không trả về dữ liệu.

### C. Sai lệch mã nguồn giữa Local và Vercel
- Cần xác nhận xem code trên Vercel đã là bản mới nhất (có sử dụng logic Go) hay vẫn là bản Python cũ. Tuy nhiên, `request_id` trong response cho thấy đây là logic Go.

## 5. Kiến nghị hành động
1. **Kiểm tra Logs trên Vercel Dashboard**: Xem log của hàm `api/index.go` để xem có lỗi "login failed" hay "lookup failed" không.
2. **Cấu hình lại Redis**: Thay vì dùng `UPSTASH_REDIS_REST_URL`, hãy sử dụng biến `REDIS_URL` (giao thức `rediss://`) được cung cấp trong mục "NPM" hoặc "Go" của Upstash Console.
3. **Debug Scraping**: Thêm log để in ra nội dung HTML nhận được từ Portal khi kết quả rỗng để xác định Portal trả về trang gì (Block, Captcha hay Login).

---
*Ghi chú: Hiện tại tôi chưa chỉnh sửa code theo yêu cầu của bạn. Tôi sẽ đợi chỉ thị tiếp theo sau khi bạn xem bản báo cáo này.*
